# Audio files

Place legally obtained audio files here:

- `haruhikage.mp3` / `.m4a` / `.wav` / `.ogg`
- `hitoshizuku.mp3` / `.m4a` / `.wav` / `.ogg`

The game reads the real audio duration and analyzes BPM after loading.
